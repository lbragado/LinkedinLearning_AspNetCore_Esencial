@page
@model ListaCursos.Pages.CoursesModel
@{
}

<table>
    @foreach (var course in Model.Courses)
    {
    <tr>
        <td>@course.Id</td>
        <td>@course.Name</td>
        <td>@course.Author</td>
    </tr>
    }
</table>